<h1>Lists#index</h1>
<p>Find me in app/views/lists/index.html.erb</p>

<% @lists.each do |list| %>
  <p><%= link_to "#{list.name}", list %></p>
<% end  %>

<%= link_to 'create a new list', new_list_path, class: 'btn btn-outline-warning' %>
